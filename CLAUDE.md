# Project: Variable-Bead Silicone DIW Flow Predictor + iLQR FFEC

## Project Overview
Physics-informed hybrid flow model for variable-bead direct ink writing (DIW)
of high-viscosity silicone. Combines an analytical ODE model (currently via 
MATLAB ode15s) with a PyTorch LSTM trained on the analytical model's residual 
error. Goal is to integrate this into an iLQR-based feedforward error 
compensation controller (FFEC) to produce optimized [Q_cmd, w_cmd] trajectories.

## Repository Structure
```
vbn-flow-model/
│
├── CLAUDE.md                        # Project instructions for Claude Code
├── VBN_flow_model_solver.m          # MATLAB ode15s ODE solver (do not modify)
├── config.json                      # WandB run config snapshot (hidden_size, num_layers, …)
├── environment.yml                  # Conda env spec
│
│── ── Core ML / Physics modules (original — do not modify) ──
├── flow_predictor_analytical.py     # Python→MATLAB bridge (per-call engine, original)
├── flow_predictor_lstm.py           # Windowed batch LSTM inference (original)
│
│── ── iLQR FFEC modules (new, March 2025) ──
├── matlab_bridge.py                 # Persistent MATLAB engine singleton (MatlabBridge)
├── lstm_step.py                     # Step-mode LSTM wrapper with (h,c) threading
├── dynamics.py                      # HybridState dataclass + HybridDynamics
├── cost.py                          # Quadratic cost + analytic Jacobians (Gauss-Newton)
├── ilqr.py                          # HybridLinearizer + ILQRSolver (FD + autograd)
├── run_ffec.py                      # CLI entry point for full iLQR FFEC run
├── validate_modules.py              # Standalone validation tests (Tests 1, 2, 3)
│
│── ── Model architecture ──
├── models/
│   ├── traj_WALR.py                 # WalrLSTM + LightningModule + DataModule (active)
│   ├── traj_WALO.py                 # Earlier WALO variant (not in active use)
│   └── traj_NALO.py                 # Earlier NALO variant (not in active use)
│
│── ── Training / sweep scripts ──
├── traj_WALR_train.py               # Training entry point for WalrLSTM
├── traj_WALR_sweep.py               # WandB sweep launcher
├── traj_WALR_test.py                # Evaluation script for WALR
├── traj_WALO_sweep.py
├── traj_NALO_sweep.py
├── traj_NALO_test.py
├── LSTM_datagen.py                  # Generates raw_sim_samples via MATLAB ODE
├── LSTM_data_splitter.py            # Splits base samples into recursive windowed samples
├── CM_sim.py                        # Continuous manufacturing simulation helper
├── vbn_flow_model_test.py           # Sanity checks on MATLAB ODE solver
│
│── ── Constants (physical parameters) ──
├── constants/
│   ├── fluid_DOW121.py              # DOW Corning 121 silicone: K_INDEX, N_INDEX
│   ├── mixer_ISSM50nozzle.py        # Active mixer nozzle: L_NOZ, KG, D_IN, D_MIX, KL_SM
│   ├── mixer_ISSM160.py
│   ├── mixer_pipe160.py
│   ├── pump_viscotec_outdated.py    # Active pump: EXTRUSION_RATIO = 9.54e-9 m³/rad
│   ├── pump_viscotec.py
│   ├── pump_pioneer.py
│   ├── pump_pioneer_outdated.py
│   ├── filepath.py                  # Shared path constants
│   └── plotting.py                  # Shared matplotlib style
│
│── ── Data (PROTECTED — never modify) ──
├── dataset/
│   ├── LSTM_sim_samples/            # 36 base simulated trajectories (.npz)
│   │                                # Keys: time, Q_com, Q_vbn, Q_res, Q_sim [, W_com]
│   ├── recursive_samples/           # 5,550 windowed sub-sequences (.npz) — TRAINING DATA
│   │                                # Generated by LSTM_data_splitter.py from LSTM_sim_samples
│   │                                # These are the files listed in splits/train.txt / val.txt
│   ├── raw_sim_samples/             # 36 raw ODE outputs before post-processing
│   ├── LSTM_exp_samples/            # 114 experimental (printed) samples
│   └── raw_exp_samples/             # 34 raw experimental captures
│
│── ── Train / val / test splits ──
├── splits/
│   ├── train.txt                    # 1,977 filenames → all in dataset/recursive_samples/
│   ├── val.txt                      # validation split  → dataset/recursive_samples/
│   └── test.txt                     # test split        → dataset/recursive_samples/
│
│── ── Input trajectory generators ──
├── inputs/
│   ├── corner_naive.py
│   ├── pathgen.py
│   └── twostep.py
│
│── ── Saved checkpoints (WandB sweep outputs — never overwrite) ──
├── VBN-modeling/                    # ~80 run dirs, each: <run_id>/checkpoints/epoch=*.ckpt
│   └── <run_id>/checkpoints/        # Active sweep: mnywg829 (entity: jplorenz-umich)
│
├── wandb/                           # WandB run metadata / logs
├── archive/                         # Legacy experiments (do not use)
└── .claude/                         # Claude Code settings
```
> **Note on data protection:** The rule "NEVER modify anything in `data/`" refers to the `dataset/`
> directory (the project uses `dataset/`, not `data/`).

## Critical Data Protection Rules
- NEVER modify, overwrite, or delete anything in data/
- NEVER overwrite saved model checkpoints in models/ without explicit instruction
- NEVER modify the original flow_predictor py files directly — create new files or
  refactored versions with clear names (e.g., flow_predictor_step.py)
- Always confirm before running any script that writes to disk

## Coding Conventions
- Python 3.x, PyTorch for ML, NumPy for array ops
- MATLAB engine calls are managed through flow_predictor_analytical.py — do not 
  instantiate matlab.engine elsewhere
- All new modules should have a brief docstring describing inputs/outputs
  and units (flowrate in mL/min, width in mm, time in seconds — confirm these)
- Use type hints on all new function signatures

## Active Development Goals (in priority order)
1. Refactor flow_predictor.py to expose step-by-step LSTM interface with 
   accessible hidden state
2. Implement dynamics_step() wrapping MATLAB + LSTM for iLQR linearization
3. Implement iLQR solver (finite differences linearization — Path 1)
4. Validate on single corner/U-turn feature against known defect profile
5. (Stretch) Replace MATLAB ODE solver with JAX/diffrax stiff solver

## Known Issues and Context
- MATLAB ode15s is used because a previous JAX ODE implementation hung —
  suspected cause is stiffness (fast acoustic timescale vs slow viscous 
  timescale). Stiff solver (diffrax Kvaerno5 or ImplicitEuler) not yet tried.
- LSTM currently runs in batch mode (full sequence in, full sequence out).
  Needs refactor to expose single-step mode with (h, c) state threading.
- iLQR linearization will use finite differences through the full hybrid 
  pipeline due to MATLAB not being autodiff-able.

## What Claude Code Should NOT Do
- Do not refactor or rename existing working modules without being asked
- Do not run training scripts without explicit instruction
- Do not commit to git without explicit instruction
- Do not install new packages without confirming first
- Do not modify CLAUDE.md itself unless asked